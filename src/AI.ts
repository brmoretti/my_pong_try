import { Board } from './Board'
import { Ball } from './Ball'

export class AI {
	static readonly timer: number = 1000;
	aiTargetY: number = Board.height / 2;
	aiTimer = 0;

	private _predictTopCollision(ball: Ball) {
		ball
	}
}
